#include "main.h"

/**
 * _abs - computing the absolute values of integer
 * @i: integer value to computed
 * Return: absolute value
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
