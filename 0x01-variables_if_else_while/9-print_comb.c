#include <stdio.h>

/**
 * main - main of the program
 *
 * Return: always 0
 */

int main(void)
{
	int n;

	n = 0;
	while
		(n < 10) {
			putchar(n + '0');
			putchar(',');
			putchar(' ');
			n++;
		}
	putchar('\n');
	return (0);
}
