#include <stdio.h>

/**
 * main - main entryof the program
 *
 * Return: always return 0
 *
 */
int main(void)
{
	char alpha;

	alpha = 'a';
	while
		(alpha <= 'z') {
			putchar(alpha++);
		}
	return (0);
}
