#include <stdio.h>

/**
 * main - the beginning of the program
 *
 * Return: njalo u 0
 *
 */

int main(void)
{
	char alph, ABET;

	alph = 'a';
	ABET = 'A';
	while
		(alph <= 'z') {
			putchar(alph);
			alph++;
		}
	while
		(ABET <= 'Z') {
			putchar(ABET);
			ABET++;
		}
	putchar('\n');

	return (0);
}
