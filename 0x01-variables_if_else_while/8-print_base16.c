#include <stdio.h>

/**
 * main - the start of the program
 *
 * Return: always 0
 */

int main(void)
{
	char n, c;

	n = '0';
	c = 'a';
	while
		(n <= '9') {
			putchar(n);
			n++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
