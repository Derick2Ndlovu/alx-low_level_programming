#include <stdio.h>

/**
 * main - ukuqala kwe program
 *
 * Return: buyisa njalo u 0
 */

int main(void)
{

        char alpha;

	alpha = 'a';
	while
		(alpha <= 'z') {
			if (alpha == 'e')
				alpha++;
			else if (alpha == 'q')
				alppha++;
			else
			putchar(alpha);
			alpha++;
		}
	putchar('\n');
	return (0);
}
