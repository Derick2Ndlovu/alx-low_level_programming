#include <stdio.h>

/**
 * main - ukuqala kwe program
 *
 * Buyisa: buyisa njalo u 0
 */

int main(void)
{

	char alpha;

	alpha = 'a';
	while
		(alpha <= 'z') {
			putchar(alpha);
			alpha++;
		}
	putchar('\n');
	return (0);
}
