#include <stdio.h>

/**
 * main - the start of the program
 *
 * Return - njalo u 0
 */

int main(void)
{
	int n;

	n = 0;
	while 
		(n < 10) {
			printf("%d", n);
			n++;
		}
	putchar('\n');
	return (0);
}
