#include <stdio.h>

/**
 * main - the main of the program
 *
 * Return: always 0
 */

int main(void)
{
	char n;

	n = '0';
	while
		(n <= '9') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
